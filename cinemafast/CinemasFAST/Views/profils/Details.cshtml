@model CinemasFAST.Models.profil

@{
    ViewBag.Title = " ";

    if (Session["login"] != null && !Session["type"].Equals("admin"))
    {
        string id = Session["id"].ToString();
        // TODO : Doit vérifier que l'utilisateur qui tente de visiter la page est le propriétaire du compte qu'il tente de visiter.
        // var account = Model.FirstOrDefault(a => a.);
        if (@Model.id != Convert.ToInt32(Session["id"]))
        {
            Response.Redirect("~/Home/Index");
        }
    }
}
    <div class="jumbotron">
        <h2>Details</h2>
    </div>


<div>
    <h4>profil</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.username)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.courriel)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.courriel)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.type)
        </dd>

    </dl>
</div>


<p>

    @if (Session["type"].Equals("admin"))
    {
        @Html.ActionLink("Retour", "Index", null, new { @class = "btn btn-dark" });
    }
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }, new { @class = "btn btn-success" })
</p>
